/**
 * Cloudflare Workers Configuration
 * TeleCore AI - Autonomous Telegram Channel Manager (Foundation Phase)
 * Target: $0/month Free Tier
 */
{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "telecore-ai",
  "main": "src/index.ts",
  "compatibility_date": "2024-09-23",
  "compatibility_flags": [
    "nodejs_compat"
  ],

  // Serve the React/Vite Dashboard UI from the compiled dist directory
  "assets": {
    "directory": "./dist",
    "binding": "ASSETS",
    "not_found_handling": "single-page-application"
  },

  // Non-sensitive runtime variables (safe in repository)
  // NOTE: TELEGRAM_TEST_MODE, TELEGRAM_CHANNEL_ID, APP_URL, and secrets are configured via Cloudflare Dashboard
  // (Settings > Variables and Secrets) or `wrangler secret put`. Do NOT place placeholders or defaults
  // here that should be owner-controlled, as wrangler deployment from GitHub will overwrite dashboard variables.
  // When TELEGRAM_TEST_MODE is omitted here, the code safely defaults to true (test mode active).
  "vars": {
    "ENVIRONMENT": "production",
    "LOG_LEVEL": "info"
  },

  // Required Production Secrets (Configured via `wrangler secret put <NAME>` in Cloudflare):
  // - GEMINI_API_KEY
  // - TELEGRAM_BOT_TOKEN
  // - TELEGRAM_WEBHOOK_SECRET
  // - ADMIN_SECRET

  // Optional: Cloudflare KV Storage Namespace (Free Tier: 100k reads/day, 1k writes/day)
  // Create using: wrangler kv:namespace create STORAGE_KV
  // "kv_namespaces": [
  //   {
  //     "binding": "STORAGE_KV",
  //     "id": "<YOUR_KV_NAMESPACE_ID>",
  //     "preview_id": "<YOUR_PREVIEW_KV_NAMESPACE_ID>"
  //   }
  // ],

  // Observability & Logs
  "observability": {
    "enabled": true
  }
}
